{
    "tasks": [
        {
            "type": "shell",
            "label": "clang build active file",
            "command": "/usr/bin/make",
            "args": [
                // "-g",
                // "${file}",
                // "-o",
                "-C", "${fileDirname}/..",
                "debug"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}